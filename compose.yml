services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: racecalendar
      POSTGRES_USER: racecalendar
      POSTGRES_PASSWORD: racecalendar
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: